#!/usr/bin/env python3
"""Module for calculating the floor of a float."""

import math


def floor(n: float) -> int:
    """Get the floor of a float.

    Args:
        n: Float to floor.

    Returns:
        Floor of the float as an integer.
    """
    return math.floor(n) 